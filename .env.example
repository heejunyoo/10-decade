# ==========================================
# Decade Journey - Environment Configuration
# ==========================================

# 1. AI Configuration
# Google Gemini API Key (Required for 'Gemini' Mode in AI Settings)
# Get yours at: https://aistudio.google.com/
GEMINI_API_KEY=YOUR_KEY_HERE

# 3. Face Recognition Tuning (Advanced)
# Threshold for believing two faces are the same person (0.3 ~ 0.6).
# Lower = More false positives (bonds strangers). Higher = More false negatives (misses family).
FACE_SIMILARITY_THRESHOLD=0.45

# Minimum face size (0.01 = 1% of image area). Increase to ignore crowd backgrounds.
MIN_FACE_RATIO=0.015

# Confidence required to detect a face (0.0 ~ 1.0).
FACE_DETECTION_THRESHOLD=0.6

# Local Model (Auto-downloaded if needed)
# Default Vision Model: Qwen/Qwen2-VL-2B-Instruct
# No configuration needed.

# 2. Personalization
# The date to calculate "Days Together" and "Highligt Years" from.
# Format: YYYY-MM-DD
WEDDING_ANNIVERSARY=2015-01-01